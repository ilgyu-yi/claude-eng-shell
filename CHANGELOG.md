# Changelog

All notable changes to this project are recorded here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Version numbers follow [semver](https://semver.org/) 0.x (`MAJOR=0` throughout v0; see SPEC §3.5 and §18.4). The per-PR fragment contract for unreleased changes lives at [`changelog_unreleased/TEMPLATE.md`](changelog_unreleased/TEMPLATE.md); `/release X.Y.Z` consolidates the fragments into a new section here.

## [0.2.0] — 2026-06-01

### Added

- `/release X.Y.Z` skill consolidates per-PR `changelog_unreleased/` fragments into a versioned `CHANGELOG.md` section, opens a `release/X.Y.Z` draft PR, and documents the post-merge tag + GitHub Release one-liner. (#131)
- `.github/workflows/check-changelog.yml` per-PR fragment-gate workflow; `skip-changelog` label opts a PR out. Distributed to adopting repos via `/onboard-dir-mode --tier 3`. (#133)
- New `/activate [<issue-#>]` skill gates every Issue through a `status:proposed → Active` review (single or no-arg batch); `activation-reviewer` now emits `pass`/`revise`/`reject` with structured refile fields, `/file-issue` stamps `status:proposed` (full symmetry), and a new `awaiting-author` label marks reviewer handoff. `/activate-directive` becomes a one-cycle alias. (#172)
- New `auto-clear-awaiting-author.yml` GitHub Action removes the `awaiting-author` label on `issues.edited`, so the reviewer→author handoff queue stays accurate after an author edits a `revise`/`reject`'d Issue (human-facing hygiene only — does not affect the `/activate` programmatic skip). (#180)
- Completed the dir-mode type taxonomy + filing gate: added a first-class `execution` label (Execution Issues are no longer identified by label-absence; the `Parent Directive:` marker is purely the parent pointer), aligned every work-type Issue template to apply its type label + `status:proposed`, and disabled blank issues (`blank_issues_enabled: false`) so every filing channel is typed and activation-gated. `task`/`execution`/`bug` stay distinct. (#186)
- Added a Korean translation of the README as `README.ko.md`, with a reciprocal language-switcher line at the top of both files; `README.md` stays the canonical source and `README.ko.md` tracks it. (#195)
- Added the `label-parent-consistency` runtime hook matcher: `gh issue edit <N> --add-label {execution|task|bug}` is now blocked when the label contradicts the Issue body's line-1 `Parent Directive: #N` marker — `execution` without a marker, or `task`/`bug` with one. This converts #197's advisory reviewer/skill prose into enforcement that fires regardless of which agent applies the label (hooks-as-environment). Scoped to the `--add-label` edit path (the `gh issue create` path is already consistent via `/file-issue`); fail-open when the Issue body can't be resolved. Escape: `SKIP_HOOKS=label-parent-consistency SKIP_REASON='<why>'`. (#199)
- Documented the **Initiative** tier (M0 foundation, milestone *v2 consuming initiatives*): a planning-tier strategic commitment one tier above a Directive, consumed — never authored — by the shell. MISSION gains a `## Consuming Initiatives` section + a "Not an author of Initiatives" non-goal; SPEC §1.7 generalizes a Directive's parent (a MISSION section **or** an Initiative), adds the load-bearing contract (an Initiative must carry a termination condition evaluable without knowledge of the code), and documents upward-only feedback (comment / challenge / completion), same-repo location, and scope. Model only — the enforcing mechanisms (the `initiative` label, `is_initiative_issue`, the three hooks, reviewer branching, the consume/feedback flow) are delivered by later milestone-v2 Directives (M1–M4) and are explicitly marked not-yet-enforced. (#246)
- Added the **recognition layer** for the Initiative tier (M1.1, milestone *v2 consuming initiatives*): onboarding now installs the `initiative` label (the planning-tier type-key, symmetric to and mutually exclusive with `directive`), and `helpers/issue_type.sh` gains an `is_initiative_issue` predicate (cached, fail-open) plus an `issue_has_initiative_parent_marker` tri-state resolver for the line-1 `Parent Initiative: #N` marker. This makes the shell *recognize* an Initiative and an Initiative-parented Directive; the enforcement matchers, reviewer branching, and consume/feedback flow remain forthcoming (M1.2–M4). The tier-2 label set is now 13 (11 from `ensure_v3_labels.sh` + inline `directive`/`initiative`). (#249)
- Added the **enforcement layer** for the Initiative tier (M1.2, milestone *v2 consuming initiatives*): three `pre_tool_use.sh` invariants make the tier coherent. (1) **Label mutual-exclusivity** — `gh issue edit <N> --add-label initiative` on a `directive` Issue (and `--add-label directive` on an `initiative` Issue) is blocked. (2) **Directive parent-XOR** — `--add-label directive` is blocked unless the body carries exactly one parent kind: a `## MISSION fit` field XOR a line-1 `Parent Initiative: #N` marker. Both fold into the existing `label-parent-consistency` matcher (`SKIP_HOOKS=label-parent-consistency`). (3) **`initiative-readonly`** (new matcher / escape category) — a mutating `gh issue edit`/`close`/`reopen` on an `initiative` Issue is blocked; `gh issue comment` is always allowed (the shell consumes Initiatives, never mutates them). All three fail open on unresolvable predicates. Adds the `issue_has_mission_fit_field` tri-state resolver. (#251)
- Added the **reviewer layer** for the Initiative tier (M2, milestone *v2 consuming initiatives*): the type-neutral `activation-reviewer` now recognizes the `initiative` label, branches a Directive's alignment check on its parent kind (a `## MISSION fit` MISSION section vs. a line-1 `Parent Initiative: #N` marker — judged against the parent Initiative's termination condition), and gains two checks the M3 consume flow invokes — **contract-evaluability** (an Initiative's termination condition must be evaluable without code knowledge, else it is under-specified and surfaced upstream) and **extraction-faithfulness** (Directives extracted from an Initiative must decompose its termination condition without smuggling strategy). Extended in place — no new subagent — to avoid the §4.9.3 session-restart caveat. SPEC §4.9/§1.7 updated. (#254)
- Added `/consume-initiative <N>` (M3, milestone *v2 consuming initiatives*): the consume flow that turns an upstream `initiative`-labelled Issue into Initiative-parented Directive proposals, working backward from its termination condition. A prompt-driven skill — type/substrate check → contract gate (`activation-reviewer` I1; an under-specified termination condition surfaces a plain upstream comment and stops) → working-backward extraction (each proposal carries a line-1 `Parent Initiative: #N` marker, no `## MISSION fit` field) → faithfulness gate (`activation-reviewer` I2 over the whole set) → file as `status:proposed` Directives (never activated; activation stays human/reviewer-gated). Read-only on the Initiative except the one under-specification comment; bodies filed via `--body-file`. Audit category `initiative-consume`. SPEC §5.21 / §1.7. (#257)
- Added `/initiative-feedback <N> {--challenge "<reason>" | --completion}` (M4, milestone *v2 consuming initiatives*) — the upward-feedback flow that completes the Initiative tier. **Challenge** posts a scannable `## Initiative challenge` comment carrying the code-derived contradiction (a real dependency/measurability/cost the planning layer cannot see) and requests re-evaluation — it escalates, never rejecting/retiring/closing. **Completion** posts a `## Initiative completion` comment carrying the merged-Directive evidence and requests termination assessment — it never asserts the condition is met or closes the Initiative. Comment-only (`initiative-readonly` backstop); `--body-file` for the injection surface; audit category `initiative-feedback`. With this, the milestone-v2 model is fully live: recognition + enforcement + reviewer + consume + feedback. SPEC §5.22 / §1.7. (#260)
- Added a **doc-consistency smoke discipline** (EI-C of doc-integrity Directive #264, smoke §74) that greps SPEC's hand-maintained enumerables against their actual sources — the agent count vs `.claude/agents/*.md` (§74a, plus §74a2 catching stale lower-count prose anywhere), every `should_skip <cat>` in `pre_tool_use.sh` vs the SPEC §7 escape-category enumeration (§74b), and the tier-2 label count vs `ensure_v3_labels.sh` + the inline `directive`/`initiative` (§74c) — so the "enumerable-facts-as-prose" drift class (which caused #238 and several spec-internal-sweep findings) now fails smoke instead of silently rotting. Also reconciled the drifted enumerables this guards: the agent count (6→9 across §3.1/§8/§16/§17), the §7 escape-category list (added the 6 missing `directive-review`/`proposed-protect`/`trusted-filer-mutate`/`label-parent-consistency`/`initiative-readonly`/`release-review`), the §2.1 audit-category list (added `activation`/`directive-reflect`/`directive-exec-count`), the §5.19 stale "10-label" reference, the §6.1 `bypass-suspect` token-vs-category note, and the `issue_type.sh` `safe_source` fail-policy note (added the M1 initiative predicates + matcher). (#267)

### Changed

- Renamed the `directive-reviewer` subagent to `activation-reviewer` and made it type-neutral (dispatches by Issue type label: Directive vs Execution rulebook); verdict vocabulary unchanged. (#170)
- Generalized the `directive-protect` branch-creation hook to `proposed-protect`: it now blocks `git checkout -b` against any `status:proposed` Issue (run `/activate` first) as well as any Directive. (#171)
- Repurposed the label-free-filing workflow (`auto-needs-triage.yml` → `auto-status-proposed.yml`): raw GitHub-UI filings now receive `status:proposed`+`task` and enter the `/activate` gate (closing the raw-channel validation-bypass hole), and the dormant `needs-triage` label is retired. Also fixes a #172 bug where the `awaiting-author` label description exceeded GitHub's 100-char limit. (#179)
- Reconciled the CLAUDE.md "Backbone" autonomy-ceiling line with SPEC §5.7.1: it no longer reads as an unqualified "stops at PR-ready" hard ceiling, but states PR-ready is the ceiling *by default* (`attended`) and that `unattended` deliberately extends it through the `/ship` CI-wait → merge-or-park terminal step — removing a contradictory boundary signal the auto-mode classifier had cited to deny an `unattended` merge. (#201)
- Synced the satellite docs (`.claude/CLAUDE.md`, `README.md`, `README.ko.md`, `docs/DIR_MODE_FLOW.md`) to the live Initiative tier and applied the thin-pointer discipline: `DIR_MODE_FLOW.md`'s duplicate state-machine diagram collapses to a pointer at SPEC §2.1 (one diagram per fact), and its drifted prose (`/activate-directive` as a live step, "two-refine escalation", "10-label set") is reconciled to the current contract (N=3 reviewer self-escalation, §1.7 label roster). EI-D of doc-integrity Directive #264. (#268)
- README (EN + KO) restructured into a lean ~100-line front-door; the full config-toggle catalog moved to `docs/CONFIG.md`, and drift fixed (smoke assertion count 547, SPEC line count ~2,000, hook summary now names `merge-strategy` + `label-parent-consistency`, `docs/DIR_MODE_FLOW.md` linked). (#297)

### Removed

- Retired the triage classifier subagent and deprecated `/triage` to a one-cycle alias for `/activate` (its `status:proposed` gate is now `/activate`; stale-discussion surfacing moved to `/activate` batch mode). The `needs-triage` label is kept but dormant pending Phase-2 external-inbound automation. (#173)

### Fixed

- `/file-directive` skill now captures `Priority` (P0–P3) at filing time and emits a well-formed `directive-file/created` audit_log line — closes the audit-format gap that allowed malformed reasons through. SPEC §18.6 + `check-changelog.yml` header now document GitHub's workflow-modification policy. (#135)
- `/file-directive`'s `P<N>` priority label now works: `P0`–`P3` are created by the canonical `ensure_v3_labels.sh` (tier-2 set, matching SPEC §0.4 which already listed them but the script never created them), de-duplicated from `onboard_target.sh`'s inline block, and `/file-directive` degrades gracefully (warns + files without the label) if it's absent. (#185)
- README reconciled with the merged v3 / activation-gate work: Quick start and both dir-mode flows now show the mandatory `/activate <N>` step (every Issue is filed `status:proposed` and `proposed-protect` blocks `/work-on` until activation), the deprecated `/activate-directive` alias is replaced by canonical `/activate`, the dir-mode substrate section reflects the 4 gh-created Project fields (Type/Status/Priority/Parent) + user-added Iteration with `/onboard-dir-mode` as the canonical installer, the `proposed-protect` hook bullet covers any `status:proposed` Issue, and the SPEC line-count reference is refreshed. (#193)
- `task` and `execution` Issues are now classified consistently: `activation-reviewer` applies parent-fit only to the `execution` label (a `Parent Directive:` marker on a `task`/`bug` is flagged as a relabel-or-drop smell), and `/file-issue` derives the type label deterministically (parent→`execution`, standalone→`task`, `--quick`→`bug`) instead of leaving it to discretion — closing the unattended-mode path that mislabeled a standalone `task` as a parented `execution`. (#197)
- Scoped the `force-push` hook matcher so it no longer blocks force-push unconditionally: force-push to an explicitly-named non-protected branch (`git push --force-with-lease origin <branch>`) is now allowed — the normal tail of the rebase-pull flow (SPEC §13) — while a force-push naming a protected branch (`main`/`master`/`release/*`), or a bare/remote-only force-push with no explicit target, is blocked. The bare form is blocked rather than inferred from the current branch because a no-refspec push's real destination is config-dependent (`push.default`/upstream), so the branch name isn't a safe proxy; the block message tells you to name the target. Escape: `SKIP_HOOKS=force-push`. (#204)
- Fixed the SPEC §7 escape hatch being non-functional through the live Claude Code Bash tool: the leading `SKIP_HOOKS=…` env-prefix is consumed by the harness as the subprocess environment and never reaches the hook, so matchers blocked even when an escape was requested. Added a **trailing-sentinel** form — `<command>  # claude-eng:skip=<category>[,…] reason=<why>` — that survives the harness (a `#`-comment stays in the command and is ignored by the executed shell) and is the supported in-harness escape. It is parsed from the raw command before normalization, namespaced so an ordinary comment isn't mistaken for an escape, one-shot, and audited via the existing escape path; the leading env-prefix form still works where it arrives in-string and wins if both are present. (#206)
- Fixed the staged-secret commit check failing **closed** when `secret_scan.sh` is missing: an unguarded `scan_staged_secrets` call meant an undefined predicate (e.g. the session-restart helper-miss `safe_source` is designed to degrade gracefully) made `command not found` block **every** `git commit`, forcing `SKIP_HOOKS=secret` on each one — the opposite of the SPEC §6.1 fail-policy table's documented fail-open. The call is now guarded with `command -v` like its sibling matchers, so a missing helper skips the secret arm (fail-open) while `safe_source` still logs the `helper-missing` warn. (#213)
- Fixed the documented Directive **unblock** path being a dead end: a Blocked Directive is Active (`directive` + `status:blocked`, no `status:proposed`), so `/activate <N>` errored "already Active" before reaching the reviewer, and even on a pass it only removed `status:proposed` — never `status:blocked`. `/activate` is now blocked-aware: it treats `status:blocked` as a re-activatable precondition, re-runs `activation-reviewer`, and on `pass` removes whichever of `status:proposed`/`status:blocked`/`awaiting-author` are present (so the mirror reconciles Status). Batch mode still does not auto-unblock (single-mode `/activate <N>` only). (#215)
- Fixed the `/resolve-discussion --no-action` close being broken end-to-end: it ran `gh issue close --reason not_planned`, but `gh` accepts only `{completed|not planned|duplicate}` (the **space** form) and rejects the underscore — and the backing `trusted-filer-mutate` discussion arm matched the underscore too, so the gh-valid `"not planned"` form was wrongly blocked as a bare close. Standardized on the gh-valid space form across the command, SPEC §5.19, and the activate.md note; the hook matcher now tolerates both forms so it is never itself the blocker. (#216)
- Fixed the escape-hatch reference docs routing in-harness blocks to the non-functional leading `SKIP_HOOKS=…` form: ESCAPE_HATCH.md, README.md/README.ko.md, and TROUBLESHOOTING.md now present the **trailing sentinel** (`<command>  # claude-eng:skip=<category> reason=<why>`) as the in-harness escape (the leading env-prefix is consumed by the harness as subprocess env and never reaches the hook, #206). Added the `label-parent-consistency` category to ESCAPE_HATCH.md and a row to TROUBLESHOOTING.md, and replaced the stale `*test*`/`*example*` `.shellsecretignore` glob defaults in CLAUDE.md and SPEC §6.1 with the actual component-aware globs. (#217)
- Hardened four low-severity robustness/idempotency gaps in maintenance tooling: `release_consolidate.sh` now fails loudly (non-zero) if a `git rm`/`git add` in the fragment-consume loop fails (was swallowed under `set -uo pipefail`, reporting false success); `ac_closeout.sh`'s detect+convert regexes dropped the trailing-space requirement so they cover the same degenerate `- [ ]` lines the merge gate blocks on (was a gate-vs-remedy deadlock); `cwd_guard.sh` normalizes a trailing slash on registry entries in both scope loops (a trailing-slash entry silently dropped its path from scope — a fail-open on the scope guard); and `migrate_v3.sh`'s header no longer claims snapshot idempotency it never had (second-resolution timestamp). (#218)
- Fixed `trusted-filer-mutate` over-blocking the sanctioned `/resolve-discussion --no-action` path: a `--reason "not planned"` close on a `discussion`-labeled Issue authored by a trusted filer (OWNER/MEMBER/MAINTAINER/COLLABORATOR — the common maintainer-filed case) was short-circuited past discussion classification into the trusted-filer block, forcing a `SKIP_HOOKS` escape on a documented frictionless close. Discussion-tier is now classified first and independent of filer trust: a discussion close allows `--reason completed`/`"not planned"` regardless of filer and blocks bare/`duplicate`; the trusted-filer block applies only to non-discussion Issues. (#236)
- Closed the `--repo owner/name` flag-form half of the `trusted-filer-mutate` cross-repo fail-open (completes #231, which scoped only the URL selector): a `gh issue close <N> --repo OTHER/repo` close-as-not-planned/duplicate of a foreign trusted-filer Issue resolved `tf_repo` empty and evaluated trust/discussion against the *current* repo, so it proceeded unblocked when current-repo #N was untrusted or absent. The close arm now parses an explicit `--repo owner/name` (or `[HOST/]owner/name`) flag when no URL-derived repo is present and uses it for the `is_trusted_filer` / discussion-label lookups; bare-number (no flag) and URL forms are unchanged, and an unparseable `--repo` value fails soft to the current repo. (#237)
- `onboard_target.sh --tier` with no value now prints a usage diagnostic and exits 2 instead of crashing with `$2: unbound variable` under `set -u`. (#238)
- Documentation/test hygiene cluster (LOW): corrected the `/onboard-dir-mode` tier-2 label count (10/11 → the canonical 12, adding the missing `execution` label); fixed `/block-directive`'s unblock hint to reference the canonical `/activate` and "on a `pass` verdict"; corrected a stale comment claiming the trusted-filer selector regex sees a non-shlex command string (`parse_env_prefix` shlex round-trips it when python3+jq are present); and isolated the `is_trusted_filer` cache per smoke section so a rerun re-exercises the gh-query path. (#238)
- Closed the `-R` short-alias half of the `trusted-filer-mutate` cross-repo fail-open (completes #237/#231): `gh issue close <N> -R owner/repo` (gh's documented short alias for `--repo`) was not parsed by the close arm, so `tf_repo` stayed empty and trust/discussion lookups resolved against the current repo — a foreign trusted-filer close-as-not-planned/duplicate via `-R` proceeded unblocked when current-repo `#N` was untrusted/absent. The close arm now parses `-R owner/name` (and `-R=owner/name`, `[HOST/]owner/name`) alongside `--repo`; bare-number/URL/`--repo` forms unchanged, unparseable values fail soft to the current repo. (#242)
- Corrected a CLAUDE.md doc drift: the "What hooks enforce" bullet conjoined the Edit/Write and `rm`/`mv`/`cp` matchers under one "two carve-outs" sentence, implying the `$HOME/.claude/` + `$CLAUDE_ENG_SHELL_ROOT/` carve-outs let forced destructive commands through. They don't — the destructive-command check has no carve-out (SPEC §6.1 scopes the carve-outs to the Edit/Write rows only), so a forced `rm`/`mv`/`cp` under `$HOME/.claude/` is still blocked (use `SKIP_HOOKS=out-of-scope`). Doc now matches SPEC + runtime; smoke §60g guards the alignment. No runtime behavior change. (#243)
- Made SPEC §1.7's `Parent Initiative` "symmetric / consumed-by" claim true where it should be and honest where it shouldn't (post-v2 spec-internal trace finding C1). The `issues-to-project-mirror` workflow now derives a Directive's Project `Parent` field from **either** line-1 marker — `Parent Directive: #N` or `Parent Initiative: #N` — so an Initiative-parented Directive is no longer second-class in the derived view (the `Parent` field is free-text, so the referent type is immaterial; `Parent Directive` stays a strict superset). `/reflect` and `dir-mode-post-merge` are deliberately **unchanged** and the §1.7 claim is scoped accordingly: they consume the *closing Execution Issue's* `Parent Directive` marker, and an Execution Issue is structurally never Initiative-parented (parent-XOR; a Directive never branches nor becomes a PR `closingIssuesReference`), so matching `Parent Initiative` there would be dead code — Directive→Initiative upward feedback flows through `/initiative-feedback` (§5.22). EI of doc-integrity Directive #264. (#262)
- Pinned four under-specified Initiative-tier integration invariants surfaced by the post-v2 spec-internal trace (C2–C5): SPEC §1.7 now states the Directive **create-path split** (`/file-directive` is MISSION-parented; `/consume-initiative` is the sole producer of `Parent Initiative`-marked Directives) and that an Initiative is **outside the 4-state lifecycle + `status:*` taxonomy**; `/activate` (+ SPEC §5.12) **refuses an `initiative`-labelled Issue** via a tri-state type resolution aligned with `activation-reviewer`'s §4.9 dispatch (closing the binary-vs-tri-state mis-typing gap); and SPEC §6.1 documents that `initiative-readonly`'s block beats `trusted-filer-mutate`'s `--reason completed` allowance on an Initiative close, by the **block-is-terminal** rule (not matcher ordering — verified against `pre_tool_use.sh`). smoke §75a–d lock all four. EI of doc-integrity Directive #264. (#263)
- SPEC internal-drift purge (EI-A of the doc-integrity Directive #264): the §2.1 Directive-lifecycle diagram, state table, and prose now use the canonical `/activate` (not the deprecated `/activate-directive` alias, which remains documented only at §5.12/§5.18); the §1.7 command list adds `/consume-initiative`/`/initiative-feedback` and demotes the aliases; dangling `§5.x` cross-refs resolve to §5.12; the `§1.7:309` line-number refs are de-fragilized; the §7 ac-closeout step ref is corrected (7.5→7.6; 7.5 is the unrelated checklist audit); the §1.7 hierarchy diagram no longer annotates the now-live Initiative tier "forthcoming"; and the §6.1 AC-closeout Type-awareness framing is corrected to all-or-nothing (skip only when *every* closing Issue is a Directive). `block-directive.md` alias refs updated. Smoke §61c guards the §2.1 alias purge. (#265)
- §4.9.1 activation-reviewer spec resynced to its SSOT file + Issues-only/Project invariant restored (EI-B of doc-integrity Directive #264, sweep S4 + S2 + #6): §4.9.1 "When" now lists all five caller paths (/file-directive, /activate, /revise-directive, /complete-directive, /consume-initiative via the Initiative rulebook); the stale "parent Goal" is now "MISSION fit (or a Parent Initiative marker)"; the proposal-review active-Directive list reads Issues (`gh issue list`), not the lagging Project (`gh project item-list`) — restoring the §1.7/§5.11 Issues-as-SSOT invariant; the parent-kind alignment check (#254) is added to the §4.9.1 Checks list and §5.16's "five proposal checks" is corrected. §5.2 now states /file-issue writes the `Parent Directive: #N` body marker and the mirror *derives* the Project Parent field (not a direct Project write). §2.1 documents that the 4-state machine is Directive-specific and discussion/initiative Issues sit outside it (their mirrored `Status=Active` is a derived-view artifact). §5.20 step 8 clarifies the on-merge tag+Release is maintainer-run, past the §5.7.1 unattended ceiling (not an unattended continuation). The three activation-reviewer-path commands (file-directive/revise-directive/consume-initiative) now defer to the reviewer's N=3 self-escalation instead of a contradictory "two consecutive revise". (#266)
- Closed three selector-normalization bypasses in the dir-mode enforcement matchers (audit Theme A, EI of Directive #275). The `label-parent-consistency` matcher now resolves its `gh issue edit … --add-label` selector via a shared `resolve_gh_issue_target` helper that is tolerant of selector **form** (bare / quoted / **URL** — A1, previously a silent bypass) and flag **order** (the positional issue arg need not immediately follow `edit` — A2), and threads a `-R`/`--repo` (or URL-embedded) **target repo** into the marker/label predicates so a cross-repo relabel resolves against the command's actual repo (A3); the predicates' new optional `repo` arg also keys their per-session cache by `owner/name`, so a foreign `#N` lookup can't poison the current repo's same-numbered entry. `initiative-readonly` migrates to the same helper (gaining flag-order tolerance) but deliberately stays **current-repo-only** — cross-repo Initiatives are out of scope (§1.7), so it governs only same-repo Initiatives. `trusted-filer-mutate`'s own parser is untouched. (#276)
- Closed a destructive-command bypass (audit Theme B, EI of Directive #275): the `rm`/`mv`/`cp` force/recursive out-of-scope block could be evaded by placing the flag **after** the operand — `rm /path/outside-registry -rf` (valid GNU syntax) — because the entry pre-filter required the flag to follow the verb with only other flags between. The pre-filter now recognizes the flag anywhere in the verb's argument run, including after operands, while still **stopping at a pipeline boundary** (`;` `|` `&`) so a flag belonging to another command (`rm ./ok && ls -rf`) does not trigger it (#212 cross-command anchoring preserved), and matching the flag as a whole token so a `-rf` inside a filename is not a false trigger. `check_destructive_args` (the in-scope/out-of-scope decision) was already operand-position-agnostic; only the entry pre-filter needed widening. In-scope permutations (`rm ./build -rf`) remain allowed. (#277)
- Fixed a token-boundary over-match in the `label-parent-consistency` matcher (audit Theme C, EI of Directive #275): its label regex terminated the type-token alternation with `([^a-z]|$)`, but `-` and `_` are `[^a-z]`, so a custom label that merely *starts with* a type token (`--add-label directive-foo`, `task_old`) over-matched the bare token and falsely tripped the matcher (e.g. blocking a `directive-foo` relabel as if it were the `directive` type-key). The terminator is now a true label boundary — comma / whitespace / quote / end — so suffixed labels no longer over-match, while the bare token, comma-joined lists, and the already-excluded letter-suffix case (`executionish`) are unaffected. The sibling `is_directive_issue`/`is_initiative_issue` label greps were already comma-anchored (`(^|,)directive(,|$)`) and needed no change. (#278)
- Test-integrity sweep (audit Theme E, EI of Directive #275): documented the two vacuous-pass anti-patterns — comment-satisfiable greps and silent skips on an absent target — as a discipline note in `scripts/test/smoke.sh`'s header (so the class is recognizable in review, parallel to the §74 enumeration discipline), and added a **count-guard** to §54g. The guard immediately surfaced a stale assertion: §54g claimed "all 6 template+workflow YAML files parse cleanly" but `[ -f ] || continue` over the glob meant an empty match would pass having checked nothing — and the real count had drifted to 7 (`discussion.yml` was added without updating the assertion). Reconciled to 7. The two instances found earlier (§57e comment-satisfiable grep, §57i silent missing-file skip) were already hardened under #262/#276. Theme F (parser limitations in `resolve_gh_issue_target`) filed as the deferred follow-up #283. (#279)
- Hardened the `resolve_gh_issue_target` selector parser (deferred follow-up from #276): it now tokenizes **shell-aware** via python3 `shlex` (mirroring `check_destructive_args`, with a `read -ra` fallback), so a quoted flag value containing an interior digit — `gh issue edit --body "fixes 99 things" 7 …` — no longer has its `99` mistaken for the positional issue selector (it correctly resolves `7`); and it normalizes a host-prefixed repo (`--repo github.com/o/r` → `o/r`) so the downstream `owner/name` derivation isn't `github.com`/`r` (which had caused a wrong-repo / fail-open lookup). On an unparseable command (unclosed quote — which would not execute in a real shell anyway) the helper emits an empty issue so the caller fails open. Both limitations were fail-safe before this fix (wrong-same-repo / fail-open, never a cross-target bypass); this closes them for accuracy. (#283)
- Closed the reviewer working-tree-isolation gap (surfaced during the v0.3 run): the read-only-by-intent subagents (`code-reviewer`, `security-reviewer`, `issue-reviewer`, `plan-reviewer`, `activation-reviewer`, `explorer`) share the parent session's working tree and carry `Bash`, so a tree-mutating git command inside one (`git checkout <base> -- <path>`, `restore`, `stash`, `reset`, `add`) silently reverts or stages the parent's uncommitted work — observed once as a post-commit reversion of `scripts/test/smoke.sh`. Two layers now prevent it: (a) the SPEC §1.5 norm makes **worktree isolation** the canonical way to invoke these subagents, and (b) each such agent's prompt is constrained to **read-only git only** (compare a base via `git diff <base>...HEAD` / `git show <ref>:<path>`, never `git checkout <base> -- <path>`). smoke §77 asserts the constraint is present in all six agent files. The structural backstop (merges build from the pushed commit, not the working tree) means a post-commit reversion never ships; the norm adds "git status before each commit/merge" as the catch-all for the pre-commit window. (#285)
- Added a `merge-strategy` PreToolUse matcher that enforces SPEC §5.7.1's no-ff merge mandate at runtime: `gh pr merge` to the **default branch** is blocked unless the strategy is `--merge` (`--squash`/`--rebase`/a bare merge → block, because a squash collapses the Doc→Test→Code arc into a single commit on the trunk — §1.2's load-bearing artifact). The policy was documented but unenforced, so manual `gh pr merge --squash` had been silently collapsing PR history on `main`. Squash is still **allowed** on a non-default base (topic-branch consolidation, §10.5) — the block keys on the live default branch (`gh repo view --json defaultBranchRef`), not `PROTECTED_BRANCH_PATTERN`. The matcher parses strategy first and allows `--merge` (incl. `--auto --merge`, the `/ship` form) silently with no `gh` calls (preserving the §39d silent-happy-path invariant); only a non-`--merge` strategy triggers the base/default-branch lookup, which fails open (`audit_log warn`) if `gh` can't resolve. Independent of the `ac-closeout` arm (own `should_skip` category). Escape: `SKIP_HOOKS=merge-strategy`. smoke §78a–h. (#288)
- Hardened the `merge-strategy` matcher's command parsing into a gh-flag-aware token walk (`parse_gh_merge_argv`, shell-aware via `shlex` with a `read -ra` fallback), closing three matcher-vs-gh mismatches from #288's security review (all below SPEC §6.1's guardrail threshold, but real): (A) a `--merge` appearing inside a `--body`/`--subject` *value* is no longer read as the strategy — strategy is the explicit flag token, not a substring, so `gh pr merge N --squash --body "see --merge"` correctly blocks; (B) the PR is the first *positional* token (or a `.../pull/N` URL), skipping value-taking flags' values, so `gh pr merge --body 7 --squash 5` resolves PR 5 (not 7); (C) the short merge flag `-m` is now recognized as compliant (it was wrongly blocked), and `-s`/`-r` short squash/rebase are blocked. The core §5.7.1 enforcement (block squash/rebase/bare to the default branch, allow `--merge`, allow squash on a non-default base) is unchanged; this only sharpens how strategy and PR are read from the command. (#290)
- `/file-issue` now captures a priority (`P0`–`P3`) and applies it as a label at create time, bringing it to parity with `/file-directive` — eng-mode issues no longer land priority-less (a priority-less backlog can't be triaged). Source order: an explicit `--priority P<N>` flag → else ask the user (attended) → else default `P2` (unattended, the same default `/file-directive` uses). The `P<N>` label is graceful-degradation-guarded (applied only if present on the target; else a warn and file-without-it) and the priority is recorded in the `issue-file` audit line so it survives a degraded label. An Execution Issue carries its own priority (it does not silently inherit the parent Directive's). SPEC §5.2 + `file-issue.md` document the contract; smoke §79 guards it. (The session's own task issues #283/#285/#288/#290 — filed before this — were backfilled by hand.) (#291)
- Made the Co-Authored-By trailer **version-agnostic** (SPEC §10.2). `coauthor_trailer` now emits `Co-Authored-By: Claude <noreply@anthropic.com>` instead of the version-pinned `Claude Opus 4.7 (1M context) …`, which had gone stale once the model advanced to 4.8 — pinning a version guaranteed the trailer drifts at every model bump. The toggle (env → state → default `on`) and all other §10.2 semantics are unchanged; SPEC §10.2's example + prose updated to match, and smoke §33e asserts the emitted line carries no model version so it can't silently re-drift. (#294)

### Security

- Fixed the trailing `# claude-eng:skip=` escape sentinel firing when the `#` is inside a quoted argument rather than a genuine unquoted shell comment: ordinary quoted text (a commit message, a PR-body paste, audit output quoting the sentinel) could silently set `SKIP_HOOKS` and disarm every matcher with a falsified audit reason while the shell ran the command intact. The sentinel is now honored only when its `#` is a real comment token, resolved with the same `python3`/`shlex` machinery the leading env-prefix uses; `python3` absent → sentinel not honored (fail-safe, enforcement stays armed). (#208)
- Closed a commit-gate bypass: `git commit --allow-empty` was excluded from the umbrella commit matcher, so it skipped the protected-branch block, the Conventional Commits subject check, the staged-secret scan, and the lint gate in one flag. The matcher now fires for empty commits too (branch + commit-format still apply; secret/lint are no-ops on an empty staged set). (#209)
- Fixed the shell-self-modification carve-out (`$CLAUDE_ENG_SHELL_ROOT/`) doing an early `exit 0` before the sensitive-file check, so an Edit/Write of a `.env`/`*.pem`/`credentials*` file under the shell root was not blocked — contradicting the documented "sensitive-file check fires under both carve-outs" contract. The carve-out now uses a flag (mirroring the `$HOME/.claude/` carve-out) and falls through to the sensitive-file check. (#210)
- Closed a declassify-guard bypass: the `trusted-filer-mutate` matcher blocking `gh issue edit <N> --remove-label directive` only matched the space-separated form, so the `=`-separated form (`--remove-label=directive`, valid `gh` syntax) silently dropped the `directive` label and de-classified a Directive without the required human confirm. The matcher now accepts both separator forms (optionally quoted) and a comma-joined value list where `directive` is any element (`--remove-label other,directive`, which gh honors), with a word-boundary tail so a longer label like `directive-foo` does not over-match. (#211)
- Hardened three hook matchers that recognized only one surface form of a command/label: the destructive-path guard now fires on `rm`/`mv`/`cp` with force/recursive flags in any form (`-r -f` separated, `--force`/`--recursive` long, `-i -rf` non-first — previously only a clustered `-rf` immediately after the verb); the `label-parent-consistency` matcher now detects the gated label anywhere in a comma-joined `--add-label` value; and `is_directive_issue` now anchors on comma-list boundaries (`(^|,)directive(,|$)`) so a label like `non-directive` is no longer mis-classified as a Directive. (#212)
- Broadened the staged-secret scanner's OpenAI key pattern to catch current-generation key formats (`sk-proj-…`, `sk-svcacct-…`, `sk-admin-…`, which contain `-`/`_`). The old `sk-[A-Za-z0-9]{40,}` stopped at the first `-` after the prefix, so the length floor was never met and modern keys passed the scan as a false negative. The `{40,}` floor is retained to limit false positives on short `sk-` identifiers. (#214)
- Closed a `trusted-filer-mutate` selector-evasion bypass: both sub-arms anchored the issue selector as a bare `[0-9]+`, so `gh issue edit https://…/issues/<N> --remove-label directive` and `gh issue close "<N>"` (URL or quoted-number selectors, both accepted by gh) evaded the matcher entirely — silently declassifying a Directive or auto-closing a trusted-filer Issue. The matcher now recognizes bare-number, quoted-number, and URL selectors, and the close arm normalizes the selector to a pure issue number before the `is_trusted_filer` / discussion-label lookups (which require a bare number) and the block message. (#223)
- Made the `trusted-filer-mutate` close arm repo-aware (amends #223): a foreign-repo URL selector (`gh issue close https://github.com/OTHER/repo/issues/N`) now resolves filer-trust and the discussion label against the URL's `owner/name` (via `--repo`) instead of the current directory's repo, so a foreign-repo close is evaluated against the foreign repo's issue rather than a same-numbered local one. `is_trusted_filer` gained an optional `owner/name` arg (current-repo behavior unchanged when omitted); a bare-number selector is unchanged and an unparseable URL repo fails soft to the current repo. (#231)
- Closed a sensitive-file guard evasion: the Edit/Write `.env`/`*.pem`/`credentials*` check matched only the **lexical** basename of the raw target, so a symlink with an innocuous name pointing at a sensitive file (e.g. `ln -s ~/.ssh/id_rsa ./innocent` then editing `./innocent`) wrote through the link unblocked. When the final path component is a symlink the check now also matches the `python3`-realpath-resolved basename (blocks if either the lexical or resolved name is sensitive); `python3` absent/unresolvable falls back to lexical-only (fail-soft). TOCTOU and hardlinks remain out of scope. (#234)

## [0.1.0] — 2026-05-26

Inaugural tagged release. Covers `init` through `v0.1.0` as a single block (per Directive #128 non-goal — no per-milestone backfill).

### Added

- Initial shell scaffolding — entry binary, bootstrap, injection, and registry of registered target paths. (#1)
- Hook system with five Claude Code event hooks (`PreToolUse` / `PostToolUse` / `Stop` / `UserPromptSubmit` / `SessionStart`) and fourteen helpers under `.claude/hooks/`. (#3)
- PR / issue / mission / ADR templates with section ownership and curate-on-commit discipline. (#5)
- Ten subagents — `planner`, `explorer`, `doc-writer`, `test-writer`, `code-reviewer`, `security-reviewer`, `issue-reviewer`, `plan-reviewer`, `directive-reviewer`, `triage-reviewer`. In `unattended` mode the six reviewers substitute for human-confirm checkpoints. (#7, #44)
- Slash commands — `/onboard`, `/file-issue`, `/work-on`, `/sync-pr`, `/status`, `/review`, `/ship`, `/adr`, `/audit`. (#9)
- CI workflow (`.github/workflows/ci.yml`) with syntax (`bash -n` + `shellcheck`) and smoke jobs across a dual-OS matrix (`ubuntu-latest` + `macos-latest`). (#13, #17, #21)
- Dir-mode v0 — Final Goal → Directive → Execution Issue layer with `directive-reviewer` subagent, five dir-mode commands (`/file-directive`, `/activate-directive`, `/list-directives`, `/link-directive`, `/complete-directive`), and type-aware engineering hooks. (#42, #43, #44, #45, #46)
- Dir-mode commands `/revise-directive` and `/block-directive` for in-place Directive body edits and Blocked-state lifecycle. (#78, #80)
- `/reflect` skill posts a Directive-progress comment on the parent Directive after each Execution PR merges. (#47, #57)
- `/triage` skill — binary accept/reject classifier for `needs-triage` / `status:proposed` Issues, backed by `triage-reviewer`. (#94)
- Discussion-tier Issues (`/discuss`, `/resolve-discussion`) — friction-free filing for "weird but not a bug" observations; bypasses the rationale-triad gate. (#112, #116)
- `/onboard-dir-mode` tier-aware target-substrate installer (tier 1 / 2 / 3) — installs the 10-label v3 set, Issue templates, workflows, and Project v2 into adopting target repos via a PR (ADR-0004). (#114, #118)
- User-global memory carve-out — Edit/Write under `$HOME/.claude/` is allowed by the registry / scope guard so the auto-memory tier works without weakening the shell's user-global-isolation contract. (#91)
- Filer-aware mutation invariants — `trusted-filer-mutate` hook arm blocks `gh issue close` on a trusted-filer Issue without `--reason completed` and blocks `gh issue edit --remove-label directive` on any filer. (#95)
- `dir-mode-post-merge` GitHub Actions workflow runs Directive-status maintenance (Active → Completed via `/complete-directive`, Status field mirroring) on every Execution PR merge. (#63)
- `claude-eng --version` short-circuited before registry resolution, sourcing from the top-level `VERSION` file with `git describe --tags` fallback. (#123)
- Annotated tag `v0.1.0` marking the first tagged release (`v` + semver convention locked by Directive #122). (#126)

### Changed

- Dir-mode v3 substrate reframe (ADR-0003) — collapsed the Goal-as-Item Project artifact into doc-as-code `MISSION.md`; Issues are the SSOT, Project v2 is a derived view; Directives use a four-state lifecycle (Proposed / Active / Blocked / Completed) encoded as labels with the Project Status field mirrored by `issues-to-project-mirror.yml`. (#96)
- Audit log format v3 — structured JSON-line format with stable field ordering and category/event tokens audit-callers can grep. (#108)
- AC-closeout matcher skips when every closing issue carries the `directive` label — Directives complete via `/complete-directive`, not AC checkboxes. (#46)

### Fixed

- macOS 3.2 bash compatibility — removed `globstar` and equivalent bash-4-only patterns; smoke + syntax run identically on Linux and macOS legs. (#17)
- Shellcheck warnings across hooks and helpers — real bugs plus justified inline silences in `.shellcheckrc`. (#21)
- Secret-scan emits per-line `<file>:<line>: <pattern-id>` markers and honors a `.shellsecretignore` allow-list at the target-repo root (gitignore-narrow). (#25)
- `SessionStart` banners cover two silent-no-op states: shell injected without `CLAUDE_ENG_SHELL_ROOT` set, and `CLAUDE_ENG_SHELL_ROOT` set but `hookrt.sh` missing. (#23, #37)
- `safe_source` utility centralizes hook-to-helper and helper-to-helper sourcing; missing helpers fail-open with one `audit_log warn <category> helper-missing` record. (#31, #34, #36)
- Matcher pass-through invariant — `pass_through_trace` audits every `pre_tool_use.sh` matcher that fell through without a decided state; high-frequency happy paths call `mark_allow` for silent ack. (#33)
- AC-closeout race / misfire fixes — split `gh pr merge` and `ac_closeout.sh` invocations to avoid GitHub API propagation latency. (#83, #29, #31)
- `setup_project.sh` reconciles `SINGLE_SELECT` options additively on re-run; preserves user-added options. (#76)
- Post-v3 drift sweep — README subagent counts, MISSION-fit narratives, and skill metadata aligned to the v3 substrate flip. (#120)

### Security

- Sensitive-file editing (`.env`, `*.pem`, `credentials*`) blocked by `PreToolUse` hook regardless of carve-outs. (#3, #25)
- Staged-diff scan for known secret formats (AWS, GitHub, GitLab, OpenAI/Anthropic, Slack tokens, password literals). (#25)
- Shell audit log never persists diff bodies — only structured records of decisions, categories, and reasons. (#3)

[0.1.0]: https://github.com/ilgyu-yi/claude-eng-shell/releases/tag/v0.1.0

[0.2.0]: https://github.com/ilgyu-yi/claude-eng-shell/releases/tag/v0.2.0
