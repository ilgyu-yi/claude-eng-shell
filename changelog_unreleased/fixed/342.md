- `setup_project.sh` now names the dir-mode Project discriminator field `Item Type` instead of `Type`: GitHub reserves `Type` as a built-in Projects-v2 field name, so `gh project field-create --name Type` failed with "Name cannot have a reserved value" and broke tier-3 onboarding at Project setup. The issues→project mirror workflow (both byte-identical copies) and SPEC §1.7 are updated in lockstep. (#342)
