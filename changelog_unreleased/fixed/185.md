- `/file-directive`'s `P<N>` priority label now works: `P0`–`P3` are created by the canonical `ensure_v3_labels.sh` (tier-2 set, matching SPEC §0.4 which already listed them but the script never created them), de-duplicated from `onboard_target.sh`'s inline block, and `/file-directive` degrades gracefully (warns + files without the label) if it's absent. (#185)
