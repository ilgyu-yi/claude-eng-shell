- The dir-mode post-merge reflection resolver is now label-aware: it climbs the `Parent Directive: #N` chain to the first `directive`-labelled ancestor (depth-cap 2, cycle guard, fail-soft) instead of posting to the first marker it finds, so nested-umbrella work no longer mis-targets the umbrella issue. The resolution logic is extracted into a sourceable, smoke-tested helper (`resolve_parent_directive.sh`) shipped alongside the workflow, and a resolved-nothing run now logs a visible no-op. (#335)
