- `/recall --deep` now finds decisions recorded in comments even when the query is a multi-token natural-language phrase: the candidate search falls back to a per-token sweep when the whole-phrase search returns nothing, so the comment scan is no longer silently skipped. (#526)
