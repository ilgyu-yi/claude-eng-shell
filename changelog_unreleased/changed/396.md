- Budgeted the always-on injected tier: `.claude/CLAUDE.md`'s "What hooks enforce" section was rewritten from ~14 dense paragraphs restating SPEC §6.1 into a one-line-per-matcher pointer index (17767→10281 bytes), the §9 thin-pointer rule now binds CLAUDE.md with a `wc -c ≤ 12000` budget drift-guarded by smoke §104, and SPEC §6.5(b) was reconciled to the actual native-load behavior (#396).
