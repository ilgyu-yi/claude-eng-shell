- Renamed the shell's internal identity to **GHJig-Claude** (the repo was already `ghjig-claude`): the entry binary is now `bin/ghjig` (invoke `ghjig`), every `CLAUDE_ENG_*` environment variable is now `GHJIG_*` (e.g. `GHJIG_SHELL_ROOT`), the in-command skip sentinel is now `# ghjig:skip=<cat>`, and the dogfood `settings.json` resolves its hooks via `${CLAUDE_PROJECT_DIR}` so the rename can never disarm a running session. Clean cut — the old `claude-eng` command and `CLAUDE_ENG_*` vars are gone and fail loudly rather than silently. (#533)
