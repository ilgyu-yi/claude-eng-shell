- Retired the ambient `GHJIG_SHELL_ROOT` environment variable: hooks and scripts now self-locate the shell root via `BASH_SOURCE` and export `GHJIG_ROOT` internally, so the ambient env can never redirect which shell tree the hooks load (the cross-clone contamination vector). Migration: unset any global `GHJIG_SHELL_ROOT` export from your shell profile — it is now ignored (a once-per-session SessionStart banner names the leftover); test seams use `GHJIG_ROOT_OVERRIDE`. (#537)
