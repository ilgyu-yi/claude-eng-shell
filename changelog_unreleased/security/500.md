- The ac-closeout merge gate no longer accepts a forged `## AC closeout` comment (now requires the canonical `(resolved by PR #N)` shape from a trusted author), evaluates the correct PR when merging by URL (`…/pull/N`, including inside flag values), and detects unchecked AC written with any task-list bullet (`-`/`*`/`+`/`N.`); the `scripts/ac_closeout.sh` remedy tracks the same bullet family so there is no gate/remedy deadlock. EI-2 of the hook-enforcement hardening directive. (#500)
