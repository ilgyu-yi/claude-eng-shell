- Closed four narrower enforcement gaps: a flagless `mv`/`cp` clobbering an out-of-registry destination is now gated; the trusted-filer gate accepts the equals form `--reason=completed`; the line-1 `Parent Directive`/`Parent Initiative` markers tolerate a trailing CRLF; and `.shellsecretignore` now documents that its whitelisted paths (`*.md`, `docs/`, `tests/`, …) have no secret-scan coverage. `find -delete`/`truncate`/`>`-redirect clobbers remain documented SPEC §6.1 residuals. EI-7 of the hook-enforcement hardening directive. (#505)
