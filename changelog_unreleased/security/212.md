- Hardened three hook matchers that recognized only one surface form of a command/label: the destructive-path guard now fires on `rm`/`mv`/`cp` with force/recursive flags in any form (`-r -f` separated, `--force`/`--recursive` long, `-i -rf` non-first — previously only a clustered `-rf` immediately after the verb); the `label-parent-consistency` matcher now detects the gated label anywhere in a comma-joined `--add-label` value; and `is_directive_issue` now anchors on comma-list boundaries (`(^|,)directive(,|$)`) so a label like `non-directive` is no longer mis-classified as a Directive. (#212)
