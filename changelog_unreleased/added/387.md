- New `/flush` skill distills task-scoped session state into durable memory (PR body + `.claude/state/` record) before a context clear, so clearing is `active → archived` not `active → delete` — the flush half of the new flush → clear → reconstruct lifecycle (SPEC §3.7). (#387)
