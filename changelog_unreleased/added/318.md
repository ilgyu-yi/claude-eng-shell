- Per-project isolation is now surfaced to users: the statusline shows the bound canonical shell root and whether ephemeral state is `project-local` vs `legacy-shared`, and `claude-eng list` prints the registered target projects (advisory — workspace symlinks ∪ legacy registry). The obsolete `inject-consistency` SessionStart banner was removed (its env-unset premise died with the #312 binding; the residual no-op is undetectable from SessionStart). (#318)
