- New `resolve_work_lang` resolver (`.claude/hooks/helpers/work_lang.sh`) + SPEC §5.7.2 establish the work-language foundation: durable repo artifacts resolve a configurable work language (`CLAUDE_ENG_WORK_LANG` → `.claude/state/work-lang` → default `en`, any code accepted) separate from the conversation language. Statusline surfacing and skill integration follow in later EIs of #322. (#323)
