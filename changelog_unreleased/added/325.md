- The status block now surfaces the active work language: `/status` and the per-turn statusline show a `work-lang:` line (and `status_json` a `work_lang` key), resolved via `resolve_work_lang`, so the language of durable artifacts is visible alongside `mode`. (#325)
