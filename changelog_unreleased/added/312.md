- Per-project binding: registered targets now resolve the shell via an untracked `.claude/eng-shell-root` symlink + `$CLAUDE_PROJECT_DIR` (with hooks self-locating their root), so a plain `claude` enforces with **no global `CLAUDE_ENG_SHELL_ROOT` env / no `~/.zshrc` export**. The shell's own `settings.json` stays env-based (dogfood unchanged). (#312)
